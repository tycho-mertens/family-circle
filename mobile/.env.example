# Replace YOUR_LAN_IP with the address of the machine running the services.
# EXPO_PUBLIC values are bundled into the app: never put secrets here.
EXPO_PUBLIC_RELAY_URL=http://YOUR_LAN_IP:5080
EXPO_PUBLIC_MAP_URL=http://YOUR_LAN_IP:8090
