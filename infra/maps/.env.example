MAP_PROVIDER=local
# Set MAP_PROVIDER=hosted and supply your own key to use hosted tiles.
PROTOMAPS_API_KEY=
PROTOMAPS_ORIGIN=http://localhost:8090

# Optional, defaults shown. MAP_MAX_CONNECTIONS is clamped to 1..1024.
#MAP_PORT=8090
#MAP_MAX_CONNECTIONS=128
